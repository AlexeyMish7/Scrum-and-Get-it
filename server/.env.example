# Server environment variables (DO NOT COMMIT SECRETS)
# Copy this to server/.env and fill with real values for local dev.

# AI provider config
AI_PROVIDER=openai
# Your OpenAI API key (server-only)
AI_API_KEY=
# Optional: default model name (e.g., gpt-4o-mini)
AI_MODEL=
# Local dev: use mock responses (set to true to avoid calling OpenAI)
FAKE_AI=false

# Supabase service credentials (server-only)
# The service role key is powerful; keep it secret and never expose it to frontend.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: other operational flags
LOG_LEVEL=info
