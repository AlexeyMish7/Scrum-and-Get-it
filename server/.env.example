# ============================================================================
# SERVER ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your values before running the server
# DO NOT commit .env to version control - it contains sensitive secrets
# ============================================================================

# ----------------------------------------------------------------------------
# AI PROVIDER CONFIGURATION (Required for AI features)
# ----------------------------------------------------------------------------

# AI_PROVIDER: Which AI service to use
# Options:
#   openai    - OpenAI GPT models (recommended)
#   azure     - Azure OpenAI Service
#   anthropic - Anthropic Claude (if implemented)
AI_PROVIDER=openai

# AI_API_KEY: Your OpenAI API key (SENSITIVE - never share!)
# Get from: https://platform.openai.com/api-keys
# Each developer should use their own key for local development
AI_API_KEY=sk-your-openai-api-key-here

# AI_MODEL: Which model to use for generation
# Options (OpenAI):
#   gpt-4o-mini    - Fast, cheap, good for most tasks (recommended)
#   gpt-4o         - Most capable, higher cost
#   gpt-4-turbo    - Previous generation, still good
#   gpt-3.5-turbo  - Fastest, cheapest, less capable
AI_MODEL=gpt-4o-mini

# AI_MAX_TOKENS: Maximum response length
# Options:
#   500   - Short responses (summaries, quick answers)
#   1000  - Medium responses (most features)
#   1500  - Long responses (cover letters, detailed resumes)
#   2000  - Very long responses (comprehensive documents)
# Note: Higher values = higher API costs
AI_MAX_TOKENS=1500

# FAKE_AI: Use mock responses instead of real API calls
# Options:
#   true  - Return fake/mock data (free, fast, for UI testing)
#   false - Use real OpenAI API (costs money, slower)
# Tip: Set to true when testing UI without burning API credits
FAKE_AI=false

# ----------------------------------------------------------------------------
# SUPABASE CONNECTION (Required)
# ----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Project Settings → API

# SUPABASE_URL: Your Supabase project URL
# Same as frontend VITE_SUPABASE_URL
SUPABASE_URL=https://your-project.supabase.co

# SUPABASE_SERVICE_ROLE_KEY: Server-side admin key (SENSITIVE!)
# ⚠️ This key bypasses Row Level Security - NEVER expose to frontend!
# Use for: admin operations, background jobs, server-side data access
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------------------
# DEVELOPMENT OPTIONS
# ----------------------------------------------------------------------------

# APP_ENV: Application environment name (supports: development, staging, production)
# Used to select `.env.<mode>` files when running locally.
APP_ENV=development

# ALLOW_DEV_AUTH: Skip auth verification for local testing
# Options:
#   true  - Allow requests without valid JWT (local dev only!)
#   false - Require valid authentication (production)
# ⚠️ MUST be false in production!
ALLOW_DEV_AUTH=true

# LOG_LEVEL: How verbose the server logs should be
# Options:
#   error - Only errors (production)
#   warn  - Errors and warnings
#   info  - General information (recommended for dev)
#   debug - Everything, very verbose (debugging only)
LOG_LEVEL=info

# CORS_ORIGIN: Which frontend origin is allowed to call this server
# Examples:
#   http://localhost:5173
#   https://your-frontend.vercel.app
# Use "*" only for local testing.
CORS_ORIGIN=http://localhost:5173

# FEATURE_AI_ROUTES: Feature flag to enable/disable AI generation endpoints
# Options:
#   true  - AI endpoints enabled (default)
#   false - AI endpoints disabled (useful for staged rollout)
FEATURE_AI_ROUTES=true

# ----------------------------------------------------------------------------
# SERVER PORT (Optional)
# ----------------------------------------------------------------------------
# PORT: Which port the server runs on
# Options:
#   8787 - Default for this project
#   Any available port
PORT=8787

# ============================================================================
# SECURITY CHECKLIST
# ============================================================================
# Before deploying to production:
# ☐ Set ALLOW_DEV_AUTH=false
# ☐ Set FAKE_AI=false
# ☐ Use production Supabase keys
# ☐ Store secrets in deployment platform (not in files)
# ☐ Set LOG_LEVEL=error or warn
# ============================================================================
