import React from "react";

const ConversionChart: React.FC = () => {
  // Placeholder visual; insert charting library later
  return (
    <div style={{ border: "1px solid #eee", padding: 12, marginBottom: 16 }}>
      <h3>Conversion Rate Over Time</h3>
      <p>(Chart placeholder — integrate `recharts` or `chart.js` for visuals)</p>
      <div style={{ height: 140, background: "linear-gradient(#fafafa,#fff)", borderRadius: 6 }} />
    </div>
  );
};

export default ConversionChart;
