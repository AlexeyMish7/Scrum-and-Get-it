import CoverLetterEditor from "../CoverLetterEditor";

/**
 * GenerateCoverLetter - Page wrapper for Cover Letter Editor
 *
 * This is the route entry point for /ai/cover-letter that renders
 * the full CoverLetterEditor component with AI generation and preview.
 */
export default function GenerateCoverLetter() {
  return <CoverLetterEditor />;
}
