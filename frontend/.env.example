# Frontend environment variables (do NOT commit real secrets)
# Copy to .env and fill values before running `npm run dev`

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key-here
# AI base URL (local dev orchestrator or deployed server)
VITE_AI_BASE_URL=http://localhost:8787

# Notes:
# - Never put service role keys or private API keys in frontend env files.
# - If AI backend requires auth beyond X-User-Id, implement secure token exchange.
