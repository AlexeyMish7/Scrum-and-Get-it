# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your values before running `npm run dev`
# DO NOT commit .env to version control - it contains sensitive data
# For multi-environment setups (UC-131), also see:
#   - .env.development.example
#   - .env.staging.example
#   - .env.production.example
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONNECTION (Required)
# ----------------------------------------------------------------------------
# Get these from: Supabase Dashboard → Project Settings → API
#
# VITE_SUPABASE_URL: Your Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co

# VITE_SUPABASE_ANON_KEY: Public anonymous key (safe for frontend)
# This is the "anon" key, NOT the service_role key
# The anon key is safe to expose - Row Level Security protects data
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ----------------------------------------------------------------------------
# BACKEND SERVER CONNECTION (Required for server-backed features)
# ----------------------------------------------------------------------------
# URL where the backend server is running.
#
# Preferred variable name (UC-129):
#   VITE_API_BASE_URL
#
# Back-compat:
#   VITE_AI_BASE_URL and VITE_API_URL are still read, but new code should
#   standardize on VITE_API_BASE_URL.
#
# Options:
#   http://localhost:8787  - Local development (default)
#   http://localhost:3001  - Alternative local port
#   https://your-api.com   - Production deployment
VITE_API_BASE_URL=http://localhost:8787

# ----------------------------------------------------------------------------
# AI SERVER CONNECTION (Back-compat)
# ----------------------------------------------------------------------------
# Keep in sync with VITE_API_BASE_URL unless you intentionally split them.
VITE_AI_BASE_URL=http://localhost:8787

# ----------------------------------------------------------------------------
# BACKEND API COMPATIBILITY (Optional)
# ----------------------------------------------------------------------------
# Some legacy frontend code paths reference VITE_API_URL.
# Keep this in sync with VITE_API_BASE_URL unless you intentionally split them.
VITE_API_URL=http://localhost:8787

# ----------------------------------------------------------------------------
# DEVELOPMENT MODE
# ----------------------------------------------------------------------------
# VITE_DEV_MODE: Show developer tools and debug panels
# Options:
#   true  - Show Dev Log Panel, React Query DevTools (local dev)
#   false - Hide all dev tools (production)
# ⚠️ MUST be false in production!
VITE_DEV_MODE=true

# ----------------------------------------------------------------------------
# CACHE TIMING (Optional - React Query)
# ----------------------------------------------------------------------------
# Controls how long profile data stays cached before refetching
# Longer times = fewer API calls, but data may be stale
# Shorter times = fresher data, but more API calls
#
# VITE_CACHE_STALE_TIME_MINUTES: How long before data needs refresh
# Options:
#   0.16  - 10 seconds (testing only)
#   1     - 1 minute (aggressive refresh)
#   5     - 5 minutes (balanced, default)
#   30    - 30 minutes (minimal API calls)
#   60    - 1 hour (for stable data)
VITE_CACHE_STALE_TIME_MINUTES=5

# VITE_CACHE_GC_TIME_MINUTES: How long unused data stays in memory
# Should be >= stale time. After this, data is garbage collected.
# Options:
#   10    - 10 minutes (aggressive cleanup)
#   30    - 30 minutes (default)
#   60    - 1 hour (keep data longer)
VITE_CACHE_GC_TIME_MINUTES=30

# ============================================================================
# SECURITY NOTES
# ============================================================================
# ❌ NEVER put these in frontend env files:
#    - SUPABASE_SERVICE_ROLE_KEY (server-only)
#    - OPENAI_API_KEY (server-only)
#    - Any private API keys
#
# ✅ SAFE for frontend:
#    - SUPABASE_ANON_KEY (protected by Row Level Security)
#    - Public URLs
#    - Feature flags
#    - Cache timing settings
# ============================================================================
