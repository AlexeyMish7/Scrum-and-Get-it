# ============================================================================
# FRONTEND (Vite) — STAGING ENVIRONMENT
# ============================================================================
# Copy to: frontend/.env.staging.local (recommended) OR frontend/.env.staging
# Build/run with:
#   npm run dev:staging
#   npm run build:staging
# ============================================================================

# Supabase (STAGING project)
VITE_SUPABASE_URL=https://YOUR_STAGING_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key-here

# Backend server (staging deployment)
VITE_API_BASE_URL=https://your-staging-api.example.com

# Backend AI server (back-compat)
VITE_AI_BASE_URL=https://your-staging-api.example.com

# Optional compatibility for legacy usage in Job Import
VITE_API_URL=https://your-staging-api.example.com

# Feature flag / dev tooling
VITE_DEV_MODE=false

# Cache tuning (optional)
VITE_CACHE_STALE_TIME_MINUTES=5
VITE_CACHE_GC_TIME_MINUTES=30
