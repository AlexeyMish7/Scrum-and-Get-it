# ============================================================================
# FRONTEND (Vite) — DEVELOPMENT ENVIRONMENT
# ============================================================================
# Copy to: frontend/.env.development.local (recommended) OR frontend/.env.development
# Vite loads `.env.[mode]` automatically when you run with `--mode [mode]`.
# Never commit real secrets.
# ============================================================================

# Supabase (DEV project)
VITE_SUPABASE_URL=https://YOUR_DEV_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-dev-anon-key-here

# Backend server (local)
VITE_API_BASE_URL=http://localhost:8787

# Backend AI server (back-compat)
VITE_AI_BASE_URL=http://localhost:8787

# Optional compatibility for legacy usage in Job Import
VITE_API_URL=http://localhost:8787

# Feature flag / dev tooling
VITE_DEV_MODE=true

# Sentry (optional)
VITE_SENTRY_DSN=
VITE_SENTRY_TRACES_SAMPLE_RATE=0

# Cache tuning (optional)
VITE_CACHE_STALE_TIME_MINUTES=5
VITE_CACHE_GC_TIME_MINUTES=30
